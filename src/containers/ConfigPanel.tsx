import * as React from "react"

export class ConfigPanel extends React.Component {
    render = () => {
        return <div>
            <span>Game mod(s)</span>
            <button>+</button>
        </div>;
    }
}